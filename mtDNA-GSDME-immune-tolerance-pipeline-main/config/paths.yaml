# ================================================
# Paths for mtDNA-GSDME Immune Tolerance Pipeline
# ================================================

# Root directory inside Docker
base_dir: "/workspace"

# Raw data directories
data_raw: "data_raw"
scrna_raw: "data_raw/scRNA"
spatial_raw: "data_raw/spatial"

# Processed data directories
data_processed: "data_processed"
scrna_processed: "data_processed/scRNA"
spatial_processed: "data_processed/spatial"

# Output & results
results: "results"
figures: "results/figures"
logs: "results/logs"

# Reference files
reference: "reference"
gene_sets: "reference/gene_sets"
markers: "reference/markers.csv"

# Credentials (not committed)
credential_file: "~/.mtDNA_pipeline/credentials.yaml"
